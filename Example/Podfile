source 'https://cdn.cocoapods.org/'


use_frameworks!

platform :ios, '15.0'

target 'TikiClient_Example' do
  pod 'TikiClient', :path => '../'
  
  pod 'TikiSdkDebug', '3.0.0', :configurations => 'Debug'
  pod 'TikiSdkRelease', '3.0.0', :configurations => 'Release'

  target 'TikiClient_Tests' do
    inherit! :search_paths

    
  end
end
